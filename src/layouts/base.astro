---
import Head from '~/components/head.astro'
import Footer from '~/components/footer.astro'
import '../styles/global.css'
const { title, description, image } = Astro.props
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<title>{title}</title>
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<Head title={title} description={description} image={image} />
	</head>
	<body class="bg-background text-foreground break-words antialiased">
		<div class="px-6">
			<main
				class="mx-auto flex min-h-[calc(100dvh_-_var(--footer-height))] max-w-(--content-width) flex-col py-12 pb-10 lg:py-32"
			>
				<slot />
			</main>
		</div>
		<Footer />
	</body>
</html>
